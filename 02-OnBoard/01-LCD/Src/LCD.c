#include "Std_types.h"
#include "BIT_MATH.h"

#include "GPIO.h"

#include "LCD.h"
#include "LCD_Private.h"
#include "LCD_Cfg.h"
#include "LCD_Lcfg.h"

void LCD_Init(void)
{
	/*Initializing LCD DATA && CONTROL Ports && Pins*/
}

ErrorStatus LCD_Display(uint8_t *Data)
{
	/*Transmitting the following data buffer to be displayed on LCD*/
	/*return error if Data = NULL*/
	/*return Ok if Date is sent correctly*/
}
